The following steps are required to launch our interface successfully:
1: Open the R file we uploaded with the name ui
2: Run the shiny app before, you need to Run app options, tick Run External option, this is to Run the shiny app in the system's default Web browser, please ensure that your default browser is Google Chrome
3: Run the shiny app
4: If the interface does not respond in time when using the "Places to drink" page, please refresh the interface，then you can use it.
